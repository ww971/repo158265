# repo158265

## G³ówne informacje
Program jest prostym skryptem do obliczania funkcji sinusa.


## Technologie
Projekt utworzony przy pomocy:
Notepad++, CodeBlocks
https://www.w3schools.com/cpp/


## Skrypt

```
#include "mysin.h"
#include <iostream>
#include <conio.h>
#include <math.h>
#include "mysin.h"
using namespace std;
int main()

{
    void MySin; //deklaracja w klasie

public:
    double MySin( double x ) //definicja klasy publicznej
    {
        cout<<"podaj x"<<endl; //proœba o podanie x
        cin>>x; //podanie x
	}
public:
    double MySin( const MySin &obj ) //definicja klasy publicznej
    {
	}
public:
    double MySin( const MySin &obj ) //definicja klasy publicznej
    {
	}
public:
    ~~MySin() //definicja klasy publicznej
    {
	}
public:
    double value() //definicja klasy publicznej
    {
    int n=2k+1; //wyra¿enie do liczenia silni
    long silnia = 1; //zmienna liczaca silnie
    cout<<"podaj k"<<endl; //proœba o podanie parametru k
    cin>>k; //podanie parametru k

	for(int i=n;i>1;i--) //pêtla: dla i równego n, i wiêkszego od 1 oraz zmniejszaj¹c i
		silnia*=i; //silnia = silnia * i
return 0;
    }
    {
        for (int i=0; i=>10;i++) //pêtla: dla i równego 0, i wiêkszego lub równego 10 oraz zwiêkszaj¹c i
        {
        sin(x) =(pow(-1,k))(pow(x,2k+1)/silnia) //obliczanie funkcji
        return 0;
    }
	}
public:
    void setX( double ) //definicja klasy publicznej
    {
	}
public:
    double getX() //definicja klasy publicznej
    {
	}
private:
{
	double mX //definicja klasy prywatnej
}

};

```
![Wykres sinus](http://amsi.org.au/ESA_Senior_Years/imageSenior/2d_29.png)
